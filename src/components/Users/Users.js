import teams from "../../teams"
import users from "../../users"

function Users() {
    console.log(teams)
    console.log(users)

    return (
    <div>users page page</div>
  );
}

export default Users;
